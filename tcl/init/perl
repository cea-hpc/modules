$ENV{MODULESHOME} = "/mips/tools/mips/envmodule";
		    
sub module {
	eval `$ENV{MODULESHOME}/modulecmd.tcl perl @_`;
}

if (! defined $ENV{MODULEPATH} ) {
  $ENV{MODULEPATH} = "/mips/tools/freeware/modulefiles";
}

1;
