
MODULESHOME=$MODULESHOME; export MODULESHOME
module () { eval `$MODULESHOME/modulecmd.tcl bash $*`; }

if [ "${MODULEPATH:-}" = "" ]; then
   MODULEPATH=/mips/tools/freeware/modulefiles
   export MODULEPATH
fi
