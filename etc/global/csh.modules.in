# module setup - should be sourced from every user's .login
if ($?tcsh) then
        set modules_shell="tcsh"
else
        set modules_shell="csh"
endif
source @DEFAULTPATH@/init/${modules_shell}
unset modules_shell
module load null
setenv ENV ~/.kshenv
