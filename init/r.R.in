# define module command and surrounding initial environment (default value
# for MODULESHOME, MODULEPATH, LOADEDMODULES and parse of init/.modulespath)
cmdpipe <- pipe('@TCLSH@ @libexecdir@/modulecmd.tcl r autoinit')
eval(parse(cmdpipe))
close(cmdpipe)
